{
  "name": "interact-io",
  "version": "0.0.0",
  "description": "",
  "engines": {
    "node": "10.15.3"
  },
  "scripts": {
    "build": "cd react-interact && npm --cwd react-interact install && npm --cwd react-interact build",
    "deploy": "cp -a react-interact/build/. public/",
    "heroku-postbuild": "npm build && npm deploy"
  },
  "author": "",
  "license": "MIT"
}
