{
  "name": "interact",
  "license": "MIT",
  "homepage": "https://interact-io.herokuapp.com",
  "proxy": "http://localhost:3000",
  "engines": {
    "node": "10.15.3",
    "yarn": "1.15.2"
  },
  "scripts": {
    "build": "yarn --cwd client install && yarn --cwd client build",
    "deploy": "cp -a client/build/. public/",
    "heroku-postbuild": "yarn build && yarn deploy"
  },
  "dependencies": {
    "serve": "^11.0.0"
  }
}
