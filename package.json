{
  "name": "interact-io",
  "version": "0.0.0",
  "description": "",
  "engines": {
    "node": "10.15.3"
  },
  "scripts": {
    "build": "cd react-interact && npm --cwd client install && npm --cwd client build",
    "deploy": "cp -a client/build/. public/",
    "heroku-postbuild": "npm build && npm deploy"
  },
  "author": "",
  "license": "MIT"
}
